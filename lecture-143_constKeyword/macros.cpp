#define pi 3.2456
// macros can also be function 

#define max(x,y) {x > y ? x : y} //function to find max 
//syntax tu define a macro
//easy to make changes in const value used at many places
//does not take any space
